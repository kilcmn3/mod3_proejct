class Player {
  constructor(name, pokemons) {
    this.name = name;
    this.pokemons = pokemons; //[poke1, poke2]
  }

  // get pokemonMoves() {
  //  return fetch(`${PokemonsURL}/${this.}`)
  // }
}
